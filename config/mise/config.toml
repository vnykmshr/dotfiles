# mise configuration
# Modern version manager for development tools

[settings]
# Show verbose output
verbose = false

# Automatically install missing tools
auto_install = true

# Pushes tools' bin-paths to the front of PATH
activate_aggressive = false

# Auto-install when command not found
not_found_auto_install = true

# Use mise for legacy version files (.nvmrc, .ruby-version, etc.)
legacy_version_file = true

# Default tools to install globally
[tools]
# Node.js LTS
node = "lts"

# Python latest stable
python = "latest"

# Go latest
go = "latest"

# Rust via rustup (if not managed by mise)
# rust = "latest"

# Additional tools
jq = "latest"
ripgrep = "latest"
fd = "latest"
bat = "latest"

# Language-specific settings
[settings.node]
# Default packages to install with every Node.js version
# default_packages_file = "~/.default-node-packages"

[settings.python]
# Default packages to install with every Python version
default_packages_file = "~/.default-python-packages"

[env]
# Environment variables
MISE_EXPERIMENTAL = "1"
MISE_YES = "1"

# Enable Node.js corepack for package manager management
COREPACK_ENABLE_STRICT = "0"